package ir.erfan_mh_at.android.reminder.app

import androidx.core.content.res.ResourcesCompat
import ir.erfan_mh_at.android.reminder.R

class AppFonts {
    companion object{
        val SUMMER_CALLING_FONT = ResourcesCompat.getFont(ReminderApplication.context, R.font.summer_calling)
    }
}